


/*
    <div>
    <button onClick={() => setResource("full match")}>Full Match</button>
    <button onClick={() => setResource("1st half")}>1ST Half</button>
    <button onClick={() => setResource("2nd half")}>2ND Half</button>

  </div>
  <h2>{resource}</h2>
  */